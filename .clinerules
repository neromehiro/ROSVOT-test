日本語で応答すること。
修正後、実行までして問題なく動作するかまでサポートすること。

# システムコマンド

- 日本語で応答すること。
- 実行環境はPowerShellであることに注意すること。
- 相対パスではなく絶対パスでimportすること
- コマンドを連結する際は、'&&'ではなく';'を使用すること。PowerShellでは'&&'が使用できないため。
- サーバーで使う環境変数は全て、.envのAZURE_APP_SETTINGSにjsonとして追加してください。新規で変数を追加しないでください。
- .envファイルは変更してもコミットしないでください
- 実際に実行する際は python3 相対パス.py で実行せずにpython 相対パス.pyで実行するようにしてください。サーバーの起動はpython main.pyではなくdocker compose upでお願いします。
- ファイルの先頭には"# module/sample.py" のようにファイルの相対パスを記載
- if __name__ == "__main__": を使って、ファイルをCLI実行できるようにしてください。
- DB接続などの初期化処理は、トップレベルではなく if __name__ == "__main__": 内で行ってください。→ 理由：モジュールとしてインポートされたときに実行されると不都合だから。
- トップレベル関数（例: main()）を定義し、__main__ ブロックでサンプル値を使って実行してください。
- コマンドライン引数を使わず、if __name__ == "__main__" ブロック内で変数に直接値を設定できるようにしてください。
jsonなどの.pyやreadme以外のテキストを開くときはターミナル経由で行うこと。理由はファイルが壊れているかの性があるため。
あなたが作成したべてのファイルはmoduleやdataset内に配置し、rootに置かないで


